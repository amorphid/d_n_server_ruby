<%= form_with(model: record, local: true) do |form| %>
  <% if record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(record.errors.count, "error") %> prohibited this record from being saved:</h2>

      <ul>
      <% record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :zone_id %>
    <%= form.number_field :zone_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :record_type %>
    <%= form.number_field :record_type %>
  </div>

  <div class="field">
    <%= form.label :record_class %>
    <%= form.number_field :record_class %>
  </div>

  <div class="field">
    <%= form.label :ttl %>
    <%= form.number_field :ttl %>
  </div>

  <div class="field">
    <%= form.label :rdata %>
    <%= form.text_field :rdata %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
